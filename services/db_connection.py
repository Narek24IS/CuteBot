# import sqlite3
#
# connection = sqlite3.connect("../CuteBotDB.db")
# cursor = connection.cursor()
#
# # Создаем таблицу "users"
# cursor.execute('''CREATE TABLE IF NOT EXISTS users (
#                     id INTEGER PRIMARY KEY UNIQUE,
#                     username TEXT,
#                     name TEXT
#                 )''')
#
# connection.commit()
